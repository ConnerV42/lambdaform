[package]
name = "rust-lambda"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "bootstrap"
path = "main.rs"

[dependencies]
lambda_http = "0.13"
lambda_runtime = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros"] }
